pipeline {
    agent {label 'georgeJavaNode01'}
    stages {
        stage ('login ocp') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'ocp', passwordVariable: 'password', usernameVariable: 'username')]){
               sh '''
                    oc login https://api.ocp49.palsys.com:6443 -u $username -p $password
                '''
              }
            }
        }
        stage ('login argocd') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'argocd', passwordVariable: 'password', usernameVariable: 'username')]){
               sh '''
                    argocd --insecure --grpc-web login argocd.apps.ocp49.palsys.com:443 --username $username --password $password
                '''
              }
            }
        }
        stage('Create javaapp on ocp') { 
            steps {
                 sh '''
                      argocd app create javaapp --repo https://github.com/george-palsys/CICD.git --path ArgoCD --dest-server https://kubernetes.default.svc --dest-namespace javaapp
                 '''
                }
            }
			
        stage("synsc javaapp") {
            steps {
                sh 'argocd app sync javaapp'  
            }
            }
      }
    }

